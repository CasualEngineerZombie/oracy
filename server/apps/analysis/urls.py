from django.urls import path

from rest_framework.routers import DefaultRouter

app_name = "analysis"

# Analysis endpoints are accessed through assessment URLs
# This app provides services, not direct API endpoints
urlpatterns = []
