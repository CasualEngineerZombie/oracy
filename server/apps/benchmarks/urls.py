from django.urls import path

from rest_framework.routers import DefaultRouter

app_name = "benchmarks"

# Benchmarks are internal to the system
# Admin interface can be used to manage benchmark definitions
urlpatterns = []
